{
  "name": "generic-graphql-mcp",
  "description": "Generic read-only GraphQL MCP server — schema discovery, query execution, error typing, federation ownership and entity resolution",
  "version": "1.1.0",
  "tools": [
    {
      "name": "query",
      "description": "Execute a GraphQL query and return typed result with error classification"
    },
    {
      "name": "raw",
      "description": "Send an arbitrary GraphQL POST body and return typed result"
    },
    {
      "name": "introspect",
      "description": "Return a summary of Query fields and types from the active schema"
    },
    {
      "name": "describe_type",
      "description": "Return field/arg details for a named type, with federation subgraph if available"
    },
    {
      "name": "list_subgraphs",
      "description": "Return subgraph metadata parsed from supergraph SDL"
    },
    {
      "name": "refresh_schema",
      "description": "Clear schema cache, forcing next access to re-fetch"
    },
    {
      "name": "entities",
      "description": "Resolve federation entities via _entities(representations:) pass-through"
    }
  ],
  "transport": {
    "type": "stdio",
    "command": "python",
    "args": ["-m", "generic_graphql_mcp.adapters.inbound.mcp_stdio"]
  }
}
