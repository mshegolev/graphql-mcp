"""CLI face — command-line entry point."""

from __future__ import annotations

import json
import sys


def main() -> None:
    """Minimal CLI entry point."""
    print(json.dumps({"status": "ok", "module": "{{ module_name }}"}))
    sys.exit(0)


if __name__ == "__main__":
    main()
