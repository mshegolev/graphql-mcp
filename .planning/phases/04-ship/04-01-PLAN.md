---
phase: 04-ship
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/graphql_mcp/domain/schema_service.py
  - src/graphql_mcp/ports/schema_source.py
  - src/graphql_mcp/ports/transport.py
autonomous: true
requirements: [GQL-10]
must_haves:
  truths:
    - "ruff check src/ tests/ exits 0 with no violations"
    - "pytest passes green with zero skips for error paths, mutation guard, cascade, federation, parity, FastAPI routes, MCP handshake"
  artifacts:
    - path: "src/graphql_mcp/domain/schema_service.py"
      provides: "Clean ruff-compliant schema service"
      contains: "from collections.abc import Sequence"
    - path: "src/graphql_mcp/ports/schema_source.py"
      provides: "Clean ruff-compliant schema source port"
      contains: "TYPE_CHECKING"
    - path: "src/graphql_mcp/ports/transport.py"
      provides: "Clean ruff-compliant transport port"
      contains: "TYPE_CHECKING"
  key_links:
    - from: "src/graphql_mcp/domain/schema_service.py"
      to: "src/graphql_mcp/ports/schema_source.py"
      via: "TYPE_CHECKING import"
      pattern: "TYPE_CHECKING"
---

<objective>
Fix all ruff lint violations and confirm test suite passes green.

Purpose: Achieve ruff compliance (5 violations: 4×TC001 + 1×UP035) and verify the existing 128 tests pass with zero skips — prerequisite for CI.
Output: Clean codebase passing both `ruff check src/ tests/` and `pytest`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts. Ports use these in annotations — must move to TYPE_CHECKING. -->

From src/graphql_mcp/domain/models.py:
```python
class SchemaGraph(BaseModel): ...
class QueryResult(BaseModel): ...
```

From src/graphql_mcp/ports/schema_source.py:
```python
@runtime_checkable
class SchemaSource(Protocol):
    @property
    def name(self) -> str: ...
    def fetch_schema(self) -> SchemaGraph | None: ...
```

From src/graphql_mcp/ports/transport.py:
```python
@runtime_checkable
class GraphQLTransport(Protocol):
    def execute(self, query: str, variables: dict[str, Any] | None = None) -> QueryResult: ...
    def post_raw(self, body: dict[str, Any]) -> QueryResult: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ruff TC001 + UP035 violations across ports and domain</name>
  <files>
    src/graphql_mcp/domain/schema_service.py
    src/graphql_mcp/ports/schema_source.py
    src/graphql_mcp/ports/transport.py
  </files>
  <action>
Fix all 5 ruff violations:

1. **src/graphql_mcp/domain/schema_service.py** (2 violations):
   - UP035: Change `from typing import Sequence` → `from collections.abc import Sequence`
   - TC001: Move `from graphql_mcp.domain.models import SchemaGraph` into a `if TYPE_CHECKING:` block. Add `from __future__ import annotations` (already present). Add `from typing import TYPE_CHECKING` import. Same for `from graphql_mcp.ports.schema_source import SchemaSource`.

   The `SchemaGraph` import is used in type annotations only (`self._cached: SchemaGraph | None`), and `SchemaSource` is used in the `Sequence[SchemaSource]` annotation — both are annotation-only since `from __future__ import annotations` is active.

2. **src/graphql_mcp/ports/schema_source.py** (1 violation):
   - TC001: Move `from graphql_mcp.domain.models import SchemaGraph` into a `if TYPE_CHECKING:` block. Add `from typing import TYPE_CHECKING`.

   IMPORTANT: This is a `runtime_checkable Protocol`. The `SchemaGraph` return type in `fetch_schema` is only used in annotations, and `from __future__ import annotations` is already present, so the move is safe. The Protocol class itself and `runtime_checkable` decorator must remain as runtime imports.

3. **src/graphql_mcp/ports/transport.py** (1 violation):
   - TC001: Move `from graphql_mcp.domain.models import QueryResult` into a `if TYPE_CHECKING:` block. Add `from typing import TYPE_CHECKING`.

   Same reasoning: `runtime_checkable Protocol` with `from __future__ import annotations` — `QueryResult` is annotation-only.

After fixes, run `ruff check src/ tests/` to confirm zero violations.
  </action>
  <verify>
    <automated>ruff check src/ tests/</automated>
  </verify>
  <done>ruff check src/ tests/ exits 0 with zero violations</done>
</task>

<task type="auto">
  <name>Task 2: Verify full test suite passes green with zero skips</name>
  <files></files>
  <action>
Run the full pytest suite. The 128 existing tests should pass. Codec parity tests that require Rust native extension are expected to skip if `maturin develop` has not been run — this is acceptable (orjson fallback path is what matters for pip install without Rust toolchain).

Verify:
- All non-native tests pass green
- No unexpected skips
- No warnings that indicate broken imports

If any test fails after the ruff fixes (e.g., runtime Protocol checks break), fix the root cause — the `from __future__ import annotations` + `TYPE_CHECKING` pattern is safe for `runtime_checkable Protocol` because `isinstance()` checks Protocol structural subtyping at runtime and don't need the annotation types to be importable.
  </action>
  <verify>
    <automated>python3 -m pytest tests/ -v --tb=short 2>&1 | tail -40</automated>
  </verify>
  <done>All tests pass green; codec parity Rust tests may skip (no native ext) but all other tests pass with zero skips</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan only fixes lint violations — no new trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T (Tampering) | TYPE_CHECKING imports | accept | No runtime behavior change — annotations only; verified by test suite |
</threat_model>

<verification>
1. `ruff check src/ tests/` exits 0
2. `python3 -m pytest tests/ -v` all pass green (Rust parity may skip)
</verification>

<success_criteria>
- Zero ruff violations across src/ and tests/
- Full test suite green (128+ tests, zero unexpected skips)
</success_criteria>

<output>
After completion, create `.planning/phases/04-ship/04-01-SUMMARY.md`
</output>
